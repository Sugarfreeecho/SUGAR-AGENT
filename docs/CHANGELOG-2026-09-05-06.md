# CHANGELOG — 2026-09-05/06

本批次为 `c5a97d8`（9/4）之后的 **33 个提交**，覆盖会话恢复、滚动体验、性能、安全与前端打磨，大部分来自 codex-ds 会话与本地迭代。

## 一、会话恢复重构（9/5）

- **服务端拥有式恢复**（`45c1f45`）：前端自动恢复不再启动竞争性的 `/continue?recovery=true` 生产者，只观察服务端恢复 worker 并附加观察者流；临时状态行（thinking/reconnecting）upsert 稳定化；孤儿运行宽限期处理。
- 中断运行残留解析（`bbc70b0`）：会话级通知/激活；用户截断中断尾巴后运行时探针停止报警（`mark_runs_resolved`）。

## 二、性能优化（9/5）

- **消除每轮停顿**（`8ee1e43`）：仅比较/提取小字段的快照读取改用 `read_consistent_view`（不再深拷贝整个投影）——persist_state 摘要检查、session-todo `has_active_plan`（每 ReAct 轮）、runtime_v2 key_context 加载器；全流水线计时覆盖。
- **前端性能**（`41561b3`）：prewarm hook 管线、`/sessions/state` TTL 缓存；`9dceff6` 本地 UI 性能记录器 + `bce9537` 运行时与性能测试。
- **扩展扫描 stale-while-revalidate**（`7bf6877`）：插件扫描、hook manager、静态分段。
- **hooks 短路径**（`d932588`）：前端检查短路、静默构建日志、显式 python。

## 三、流式/滚动体验（9/5–9/6，多次迭代后收敛）

- 绿点会话（运行中）加载直接落位最新结果（`dc752f6` → `077cc2a` → `88c9c76`），图片加载时历史平滑滚动稳定（`49dbd51`）；中途经历 4 次 Revert（`7157760`/`f4b0ccc`/`23356e7`/`baa02c2`）后收敛为**单一底部滑动 + 无闪烁**方案。
- 保留**工作区图片在聊天历史**（`32f582b`），图片加载不打断底部定位。

## 四、LLM 传输健壮性（9/6）

- 连接错误**就地重试后**才走模型回退（`1e24a97`）。
- 多模态回退、流损坏重试、审批原因拆分、dist 同步（`2809267`）。

## 五、安全与工具（9/6）

- **回环网络命令不再视为破坏性**（`d89724c`）：如 `curl http://127.0.0.1` 之类本地调试命令不再触发危险审批。
- **apply_patch 允许经目录授权编辑 WORK_DIR 外文件**（`13e91d4` 文档）：与 9/2 的目录授权审批联动。

## 六、前端重构与打磨（9/5）

- **废弃 v1/v2 皮肤拆分**（`b970d90`）：process 面板成为唯一皮肤。
- 主题配色微调（`a557590`）；分层术语（session/round/step/item）与不变式文档（`3c21ea3`）。
- 测试同步过期断言（`0394bb9`）、chat-completions 缓存用量回归（`58ba244`）。

## 相关提交

`8ee1e43` perf(agent) · `bbc70b0` feat(webui) · `45c1f45` fix(recovery) · `49dbd51` fix(webui) · `b970d90` refactor(frontend) · `41561b3` perf(webui) · `d932588` perf(hooks) · `1e24a97` fix(llm) · `d89724c` fix(security) · `88c9c76` fix(webui) · `32f582b` fix(images) 等
