# CHANGELOG — 2026-09-14 → 09-15

本批次为 `a783ad4` 之后的 **5 个提交**（9/14 下午 – 9/15 上午）。

## 一、右侧详情栏（Dock）— 新功能（9/15）

`a52298a` feat(webui): right detail dock (dsh dockkit single mount point)

- 参照 deepseek-harness `ui-dockkit` 实现**右侧详情栏**：会话标题右上角图标打开，向左推开工作区。
- **三层架构**：
  - embedder（产品层）：`right-column.js`（右侧轨道/拖宽柄/角落按钮/三个内置页）、`surface-store.js`（每会话停靠面）、`tab-registry.js`（标签类型注册表）
  - renderer（DOM+手势）：`dock-surface.js`、`float-layer.js`、`gesture.js`、`measure.js`、`icons.js`
  - engine（纯逻辑）：`applyOp(state, op)` 可逆操作、`dockReplay` 确定性重放（types/tree/constraints/geometry/planner/sequence/controller）
- 内置三页：**工作区文件 / 文件内容 / 修改历史**；早前的"主区分屏 + conversation 镜像"方案按要求整体移除。
- 配套：`webui.py` 支持（+53）、i18n 文案、引擎测试（**24 checks passed**）、设计文档 `docs/frontend-dock.md` 与 `08-详情栏方案设计-UseCase清单.md`。

## 二、运行态与审批修复（9/14）

- `d022831` fix(webui): **采用服务端拥有的 run**、扩展面板收敛——前端以服务端运行状态为准，扩展面板状态不再发散。
- `a301edd` fix: **shell 相对路径按 workdir 基准判定审批归属**——相对路径的审批归属以命令实际工作目录为基准。
- `4c04bb2` docs(用户方案): d022831 的评审修正。

## 三、构建

- `5ff4a0d` build: sync frontend distribution。
