# CHANGELOG — 2026-09-10 → 09-14

本批次为 `db4c920`（9/07–09/10 changelog）之后的 **11 个提交**。

## 一、图片理解管线（新，9/14）

`9db150f` feat: add durable image understanding pipeline

- 新增**持久化图片理解管线**：图片在会话中可被理解并保留（对接多模态输入模态的序列化规则；README/SPEC 同步更新）。
- 涉及 `agent_openai`（+323）、`agent_harness`、`agent_loop`、`agent_mcp`、`agent_subagent`、`agent_memory`、`agent_tokenizer` 等主链路模块。

## 二、LLM 恢复原因与告警（9/14）

`40c3430` fix: preserve LLM recovery causes and status alerts

- 保留 LLM 恢复链路的**原始失败原因**与状态告警分类；新增 `test_llm_alert_classification.py`（+68），本地网络恢复测试扩展。

## 三、改动审查（change-review）（9/10）

`3dfada4` feat: restore undone changes and fix stats display

- **撤销可恢复**：新增撤销后的恢复能力（`store.restore`/`commit_restore`/`rollback_restore`，同样的全有或全无冲突保护与幂等重放）；回退记录的快照保留至历史裁剪；新端点 `POST /sessions/{id}/change-reviews/restore` + `file_changes_restored` UI 事件。
- **统计修正**：null 增删行数计为"未测量"并给出原因细分；修复修订重置时丢弃较新运行记录的问题；忽略文件保留真实来源（撤销时恢复内容而非删除）。
- UI：已回退行常显 + Restore/Restore all 按钮；测试 7 个新后端用例 + 前端套件 44 通过。
- `a135145` fix: keep temporary writes hidden（临时写入不进入审查）。

## 四、LLM 传输与运行参数（9/10）

- `de802ce` fix(llm): **compatible 流式 usage**——通过 `stream_options` 请求用量，端点拒绝时降级重试（不带该参数）；无关错误不重试。
- `a4d94b4` feat(agent): **`MAX_REACT_ITER` / `SUBAGENT_MAX_REACT_ITER` 可配置**（默认 200）。
- `d5b4482` fix(webui): **Token/s 统计口径**——以纯生成段为分母，排除首 token 等待与网络往返。

## 五、模型表与测试基建（9/12–9/14）

- `6acc6bf` chore: 刷新 `models_table.md`（OpenRouter 快照，**445 个模型**，2026-09-12）。
- `7b5015e` test: 模型 profile 期望对齐最新模型表。
- `a1dee97` chore: 新增 **TTFT 诊断探针与报告**（首 token 延迟调查工具集）。
- `a6303db` test: 懒加载远程控制路由经 ASGI 验证。

## 相关提交一览

`9db150f` `40c3430` `a135145` `3dfada4` `6acc6bf` `a1dee97` `7b5015e` `a6303db` `de802ce` `a4d94b4` `d5b4482`
