# 工作区 · 模块整体设计

- 版本：2026-09-14 v2（覆盖至：HEAD `6acc6bf` + API 识图工作区改动）
- 用途：本模块总入口；各功能专项设计按四字段逐条审查。
- 适用实现：`app/agent_harness.py`（路径常量）、`app/agent_tools.py`（路径解析/回收站/上传）、`app/session_authorized_dirs.py`、`app/webui.py`（浏览/媒体/打开）、`frontend/src/vendor/myagent_path_picker.js`。
- 配套：架构图 `workspace/archify_study/workspace/workspace.architecture.html`；能力清单 `工作区能力清单.md`（本文件夹）。

---

## 1. 一句话定位

"机器的桌面"：虚拟路径模型、越界授权、回收站、上传/浏览/媒体——所有文件进出的公共地基。

## 2. 目标 / 非目标

**目标**
1. 一个心智模型：`/` = 工作区根；越界需授权且一次授权、会话内复用；
2. 删除永远可回收（.trash），敏感区受保护；
3. 上传/浏览/预览覆盖日常素材流转。

**非目标**
- 不做版本控制（那属于用户的 Git/改动审查插件）；不做云同步。

## 3. 功能地图

| # | 功能专项设计 | 状态 |
|---|---|---|
| 01 | [路径模型与安全解析](01-路径模型与安全解析方案设计-UseCase清单.md) | ✅ |
| 02 | [越界审批与授权目录](02-越界审批与授权目录方案设计-UseCase清单.md) | ✅ |
| 03 | [回收站与临时文件生命周期](03-回收站与临时文件生命周期方案设计-UseCase清单.md) | ✅ |
| 04 | [上传、附件对象与命名](04-上传与命名方案设计-UseCase清单.md) | ✅ |
| 05 | [浏览、媒体与图片服务](05-浏览媒体与图片服务方案设计-UseCase清单.md) | ✅ |
| 06 | [打开协议与路径选择器](06-打开协议与路径选择器方案设计-UseCase清单.md) | ✅ |

## 4. 跨功能不变式

1. **先解析后执行**：一切文件操作先过 `safe_work_path` 类解析与越界检查；Shell 相对路径按**生效工作目录**（`workdir`）解析，保证判定基准与执行基准一致（见 01/UC-4A5）。
2. **可回收承诺**：删除类操作默认进入 `.trash`（物理删除基本不发生）。
3. **会话记忆**：授权目录、可见性规则都以会话为单元，不跨会话泄漏。
4. **图片引用持久**：图片通过统一准入固化为内容寻址对象；消息与队列只保存引用，规范对象只能按可达性回收。

## 5. 边界总览

- 工具层的删除/截断细节见 ../03-工具系统/06；
- 图片的模型能力与 API 投影见 [../09-横切能力/02](../09-横切能力/02-识图与多模态投影方案设计-UseCase清单.md)；
- sessions/skills 等内部目录的**结构与生命周期**属 ../06/../08 模块，本篇只管"从文件系统视角的保护与展示"。

## 6. 版本记录

- 2026-09-14 v3：跨功能不变式补充 Shell 路径解析基准（workdir）与执行语义对齐（配合当日路径基准修复）。
- 2026-09-14 v2：纳入图片附件对象、耐久引用和可达性回收。
- 2026-09-13 v1：从总清单拆分。
