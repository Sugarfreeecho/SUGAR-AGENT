# 工作区 · 能力清单（代码证据版）

> 对象：MyAgent 工作区（WORK_DIR 路径模型、授权、回收站、上传与浏览、媒体服务）
> 代码版本：HEAD `d022831` + API 识图工作区改动（2026-09-14 扫描；含 9-14 路径基准修复）
> 图例：【图】见 `workspace.architecture.html`（10 节点）；【卡】图中卡片；【单】仅本清单

## 1. 路径模型与环境
| 能力 | 位置 | 状态 |
|---|---|---|
| WORK_DIR 解析：`_env_path("WORK_DIR", PROJECT_ROOT/"workspace")`，启动时 mkdir | `agent_harness.py` L127/L136/L248 | 【图】 |
| 虚拟「/」映射工作区根；写类工具默认以此为基准 | 系统提示 + `agent_tools.py` 头部契约 | 【图】 |
| POLICY 常量：SKILLS_DIR（WORK_DIR/skills）、SESSIONS_DIR（WORK_DIR/sessions）、LOG_DIR | `agent_harness.py` | 【单】 |

## 2. 路径解析与安全
| 能力 | 位置 | 状态 |
|---|---|---|
| 路径文字预处理：连续「/」折叠、虚拟路径消歧、Windows 盘符/UNC 字面量保护 | `prepare_agent_workspace_path_literal`、`_collapse_adjacent_slashes` | 【图】 |
| `safe_work_path` / `resolve_unrestricted_path`（受限 vs 自由解析） | `agent_tools.py` | 【图】 |
| within 检查与规范化（resolve + normcase + 去尾斜杠） | `session_authorized_dirs._normalize_dir/_is_within` | 【卡】 |
| Shell 相对路径基准：run_shell 的相对路径（含 `..`）按生效工作目录（workdir）解析，子目录 `..` 回工作区不误判越界 | `agent_tools._resolve_shell_working_dir`、`_resolve_shell_token_for_workspace_restrict`（base）、`_outside_workspace_tokens`；`security/runtime._effective_shell_base` | 【单】 |
| 敏感资源识别（API Key 存储等路径统一脱敏/拒访） | `_path_is_sensitive_tool_resource`（见工具系统清单） | 【卡】 |

## 3. 越界审批与授权目录
| 能力 | 位置 | 状态 |
|---|---|---|
| 工作区外绝对路径 → 审批卡片（受限模式），授权后写入会话元数据 | `session_authorized_dirs.add_authorized_dir`、`agent_harness` metadata `authorized_dirs` | 【图】 |
| 授权目录读取与命中：`get_authorized_dirs_for_session` / `is_path_authorized_for_session`（默认回退 [WORK_DIR]） | `session_authorized_dirs.py` | 【图】 |
| 所需授权目录计算（最小包裹目录去重）与 ensure 初始化 | `compute_required_authorization_dirs`、`ensure_session_authorized_dirs` | 【卡】 |

## 4. 回收站（软删除）
| 能力 | 位置 | 状态 |
|---|---|---|
| 软删除到 `WORK_DIR/.trash/`，时间戳前缀防重名，返回可读回执 | `delete_file`、`TRASH_DIR` | 【图】 |
| 体积护栏：合计 > `TRASH_SIZE_WARN_MB`（默认 500MB）拒绝移动并提示手动清理 | `agent_tools.py` | 【卡】 |
| 受保护路径拒删：`sessions/`、`skills/`、`.trash/` 及子路径 | `_delete_path_prohibited_reason` | 【卡】 |
| 临时文件（`temporary=true`）turn 末自动移入 `.trash`；`_RUN_SHELL_TEMP_DIR=".trash"` | `agent_tools.py`、`agent_loop.py` L8566 | 【图】 |
| 插件安装器的独立回收（`.myagent-trash`，与工作区回收站分离） | `plugins/installer.py` | 【单】 |

## 5. 上传与命名
| 能力 | 位置 | 状态 |
|---|---|---|
| 安全文件名与去重（同名自动递增） | `_safe_upload_filename`、`_dedupe_upload_path` | 【图】 |
| 普通文件上传限额与错误类型（`_ChatUploadLimitError`）、安全落盘至工作区 | `upload_chat_files` | 【卡】 |
| 图片整批准入：解码校验、归一化、内容寻址、去重、失败回滚 | `attachments/admission.py`、`normalization.py`、`local.py` | 【图】 |
| 附件授权、设备 grant、队列/作业 pin 与宽限租约 | `attachments/access.py`、`registry.py` | 【单】 |
| 远程图片安全入库与附件 ZIP 导入/导出 | `attachments/api.py`、`remote.py`、`lifecycle.py` | 【单】 |

## 6. 浏览 API
| 能力 | 位置 | 状态 |
|---|---|---|
| 文件列表/目录浏览：条目上限、隐藏可见性规则、行数统计与体积 | `_list_workspace_dir`、`_scan_workspace_files`、`_is_workspace_visible_dir` | 【图】 |
| 路径相对化（相对工作区根解析 + 越界防护） | `_resolve_workspace_rel_dir`、`_resolve_allowed_local_path` | 【单】 |

## 7. 媒体与图片服务
| 能力 | 位置 | 状态 |
|---|---|---|
| 图片元数据（维度缓存、SVG 长度像素解析） | `_workspace_image_metadata_item`、`_cached_workspace_image_dimensions`、`_svg_image_dimensions` | 【图】 |
| 媒体响应（工作区图片读取与缓存头） | `_workspace_media_response`、`workspace_media` | 【单】 |
| 耐久附件读取：ID/授权/完整性校验、ETag 与 304 | `webui.read_attachment`、`attachments/access.py` | 【单】 |
| 对象与请求缓存分级配额、可达性 GC | `attachments/local.py`、`request_image.py`、`lifecycle.py` | 【单】 |

## 8. 打开协议与选择器
| 能力 | 位置 | 状态 |
|---|---|---|
| 工作区文件打开（受限到允许的本地路径） | `open_workspace_file` | 【图】 |
| `sugaragent://` 协议与路径选择器（前端 vendor + 后端 picker） | `myagent_path_picker.js`、`api_pick_path`、`path_picker_util.py` | 【卡】 |

## 9. 边界说明
- 本模块覆盖附件对象的工作区存储、授权读取、回收和备份；模型能力门控与请求投影见 [识图与多模态投影](../09-横切能力/02-识图与多模态投影方案设计-UseCase清单.md)。
- 会话目录（sessions）内部结构归"会话存储 Runtime V2"模块清单。
