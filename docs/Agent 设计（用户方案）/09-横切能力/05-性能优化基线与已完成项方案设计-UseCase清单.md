# 性能优化基线与已完成项 · 功能方案设计（UseCase 清单）

- 版本：2026-09-20 v1（覆盖至：当前工作区）
- 用途：把 `.analysis/性能优化-目标与进度.md` 中全部历史已完成项转成用户方案的稳定索引、验收口径和模块归属。
- 适用实现：`app/agent_{loop,harness,openai,tokenizer,tools}.py`、`app/attachments/**`、`app/webui.py`、Runtime V2 与扩展加载相关实现。
- 上级：`00-横切能力整体设计.md`
- 权威进度：`../../../.analysis/性能优化-目标与进度.md`；本篇记录已经进入产品设计的行为契约，数值基线以后者最新实测为准。

---

## 1. 功能定位

性能优化不是单个“加速开关”，而是贯穿 LLM 连接、ReAct 轮间准备、附件序列化、Runtime V2、扩展刷新和文件工具的系统能力。本篇保证每个历史完成项都能从进度清单追溯到用户方案中的所属用例，并规定优化不能破坏正确性、回退、刷新和持久化边界。

## 2. UseCase

### UC-9E1 首 token 冷热路径治理

- **触发**：服务启动后的首条消息、连接池长时间空闲后的首轮，或连续调用同一模型端点。
- **预期现象**：提示构建、tokenizer 和模型连接在后台预热；流响应排空到 EOF 以复用连接；首选候选走低开销直连适配器。
- **规则与边界**：预热失败不阻断真实请求；直连不得绕过候选链、预算、熔断、取消和回退；keepalive 到期后允许正常重建连接。
- **依据**：01/07 UC-1G3~1G5，02/02 UC-2B5。

### UC-9E2 长历史本地热路径保持增量

- **触发**：历史随 ReAct 轮次追加，系统重复执行附件耐久化、思考剥离、消息哈希、文本扁平化和 token 估算。
- **预期现象**：稳定前缀复用已有结果，只处理新增尾部；附件存储配置和路径解析不按消息重复计算。
- **规则与边界**：对象身份缓存必须保存对象引用并用 `is` 比较，不能只缓存裸 `id()`；token 估算保持压缩 gate 所需精度；配置变化有显式失效入口。
- **依据**：09/02 UC-9B26，02/02 UC-2B3。

### UC-9E3 轮间状态读取与回调调度去重

- **触发**：每轮 `before_round`、工作流回调查询和 context summary 持久化。
- **预期现象**：回调注册表短时复用，Todo 活跃状态走同步过的内存态，未变化摘要跳过重复磁盘快照读取。
- **规则与边界**：run 初始化、工具/UI 更新、压缩和缓存未命中仍回到权威状态；优化不得掩盖其他写者或扩展热更新。
- **依据**：02/06 UC-2F5~2F6，08/03 UC-8C5。

### UC-9E4 工具发现与长运行收敛

- **触发**：搜索大目录、列出多文件目录、工具连续使用路径，或单次 run 进入高轮次/高工具调用数。
- **预期现象**：grep 达到结果上限即停止，ls 默认不逐文件计数，路径错误给出候选；长运行达到阈值后优先综合已有证据。
- **规则与边界**：隐藏/ignored 文件和行数统计仍可显式开启；路径候选不自动替用户改写；收敛检查点是软提醒而非硬截断。
- **依据**：03/04 UC-3D5~3D7，02/01 UC-2A5。

### UC-9E5 可归因、可回归、可失效

- **触发**：评估性能改动、修改 SKILL.md，或使用诊断旁路定位 token 分词成本。
- **预期现象**：阶段计时能区分 provider、工具与本地框架；Skills 树变化推进 catalog generation；诊断开关可隔离本地 token 估算。
- **规则与边界**：父子计时不得重复累计，各阶段中位数不得直接相加；诊断旁路只用于测量；任何缓存必须有签名、TTL、版本或显式失效边界。
- **依据**：09/04 UC-9D3/UC-9D7，06/05 UC-6E4，02/02 UC-2B6。

## 3. 历史完成项完整映射（3.1–3.19）

| 进度项 | 已完成能力 | 用户方案落点 | 关键验收结果 |
|---|---|---|---|
| 3.1 | 附件序列化超线性修复 | 09/02 UC-9B26 | 160k 层 794.2 → 82.4 ms |
| 3.2 | EOF drain + keepalive 连接复用 | 01/07 UC-1G3 | 冷/暖配对中位改善约 1,090 ms；生产复用 95/96 轮 |
| 3.3 | 首选候选直连 transport，保留 facade 回退 | 01/07 UC-1G4 | facade 3,611 ms vs 直连 2,919 ms；两路均可回退 |
| 3.4 | 提示与 LLM 连接预热 | 01/07 UC-1G5 | 0/30/120 秒复用；空闲过期后按需重建 |
| 3.5 | token 哈希最长对象身份前缀缓存 | 02/02 UC-2B3 | 每轮 22.2 → 0.5 ms |
| 3.6 | 精确增量分词 | 02/02 UC-2B3 | 最高 686×；增量最大偏差 1 token |
| 3.7 | `persist_state` 未变化摘要跳过磁盘读 | 08/03 UC-8C5 | 命中进程缓存直接返回；冷未命中仍查权威快照 |
| 3.8 | Skills 签名变化推进 generation | 06/05 UC-6E4 | 磁盘直接修改 SKILL.md 后静态段可刷新 |
| 3.9 | 工作流回调调度缓存 | 02/06 UC-2F5 | 128.576 → 0.002 ms |
| 3.10 | transport/token/workflow/stream 等性能埋点 | 09/04 UC-9D3 | 可分解服务端、连接、流间隙和本地命中路径 |
| 3.11 | 本地 token 估算诊断旁路 | 02/02 UC-2B6 | 可隔离本地分词；明确禁止默认启用 |
| 3.12 | Todo `before_round` 内存态查询 | 02/06 UC-2F6 | `has_active_plan()` 中位 0.3 µs |
| 3.13 | `pre_api.total_ms` 排除嵌套诊断区间 | 09/04 UC-9D3/UC-9D7 | 总计只累计互斥父区间 |
| 3.14 | 思考清理与扁平文本前缀缓存 | 02/02 UC-2B3 | 真实 1,244 条历史：相关路径各约 0.25 ms |
| 3.15 | grep 流式截断并默认遵守 ignore | 03/04 UC-3D6 | 原慢调用 30.2 s 超时 → 439 ms |
| 3.16 | ls 轻量默认与行数缓存 | 03/04 UC-3D5 | 225 文件目录约 9 s → 49 ms |
| 3.17 | 完整路径复用与 `Did you mean` | 03/04 UC-3D1/UC-3D7 | 降低路径拼错导致的失败轮次 |
| 3.18 | 长会话软收敛检查点 | 02/01 UC-2A5 | 24轮/48工具开始提醒，32轮/72工具升级 |
| 3.19 | tokenizer 后台预热 | 02/02 UC-2B5 | 冷预热 220 ms，随后首次计数 0.059 ms |

## 4. 当前验收基线

- 全量回归：**1648 passed / 4 skipped / 9 subtests passed**。
- 会话 `8278fdd4`：严格轮间中位 **37 ms**、p90 **129 ms**、最大 **887 ms**，`≤100 ms` 为 **32/36（88.9%）**。
- 该会话覆盖 12.4k–124.2k tokens；300k+ 长上下文仍需独立生产回归，不能用当前样本外推。
- 性能数值是硬件与会话相关基线；稳定产品契约是正确性边界、缓存失效机制和可归因指标。

## 5. 边界

- 本篇不取代各模块专项：网络、分词、工具、Skills、Runtime V2 和附件的详细行为以对应 UC 为准。
- `.analysis/性能优化-目标与进度.md` 是优化过程与最新测量真源；本篇只收录“已完成”，不把待办或假设写成现有能力。
- 后续新增已完成项时，必须同时更新权威进度、所属模块 UC 和本篇映射表，避免只有实现或只有性能报告而用户方案缺席。

## 6. 版本记录

- 2026-09-20 v1：首次完整同步历史已完成 3.1–3.19，并建立逐项模块落点、验收结果和失效边界。
