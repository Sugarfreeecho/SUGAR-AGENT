# 横切能力 · 模块整体设计

- 版本：2026-09-20 v6（覆盖至：当前工作区）
- 用途：本模块总入口。**横切能力**指不隶属单一模块、但被多个模块共同依赖的机制——压缩、识图、告警、观测与性能基线。
- 适用实现：`app/agent_memory.py`（压缩，1702 行）、`app/attachments/**`（识图）、`workspace/alert_spec_myagent/ALERT_SPEC.md`（告警）、`app/runtime_observability.py / execution_metrics.py`（观测）。
- 配套：相关章节散布于各模块清单；告警规范单独成册（ALERT_SPEC v1.0）。

---

## 1. 一句话定位

五根"贯穿全系统的线"：**上下文压缩**（对话不爆）、**识图投影**（多模态不哑）、**告警体系**（异常有声）、**观测**（运行有数）、**性能基线**（优化可追溯）。

## 2. 功能地图

| # | 功能专项设计 | 状态 |
|---|---|---|
| 01 | [上下文压缩全链路](01-上下文压缩全链路方案设计-UseCase清单.md) | ✅（告警项见 03 🟡） |
| 02 | [API 识图与多模态投影](02-识图与多模态投影方案设计-UseCase清单.md) | ✅（UC-9B1~9B26） |
| 03 | [告警分级与错误卡](03-告警分级与错误卡方案设计-UseCase清单.md) | 部分 ✅ / 部分 🟡 |
| 04 | [观测与运行看板](04-观测与运行看板方案设计-UseCase清单.md) | ✅ |
| 05 | [性能优化基线与已完成项](05-性能优化基线与已完成项方案设计-UseCase清单.md) | ✅（历史完成项 3.1~3.19） |

## 3. 跨功能不变式

1. **不打扰原则**：压缩/观测后台进行；仅用户可感知处（进度、告警）才露头。
2. **诚实原则**：压缩兜底降质、告警未实现项、观测缺失数据——全部如实标注，不伪装。
3. **耐久引用原则**：图片等大载荷保存在内容寻址对象库；消息、事件、队列、日志和独立作业只持有引用或脱敏状态。
4. **结果语义原则**：图片“已接收、已准备、已发送、模型已完成”分开表达；纯文本结果不能冒充模型看过图片。
5. **性能口径原则**：性能结论必须基于逐轮互斥区间；父子诊断段不重复累计，各阶段中位数不直接相加，provider、工具与本地框架分别归因。
6. **优化可追溯原则**：每项已完成性能优化必须同时具有进度记录、所属模块 UC、验收结果和缓存/回退边界。
7. **压缩可追溯原则**：活跃上下文允许有损，但被省略的 reasoning、工具调用与结果必须有稳定归档引用；模型应先用 `history_context` 回溯，不得把摘要当作完整事实源。

## 4. 边界总览

- 压缩与各模块的接口：触发（../02/07）、编排（../02/05）、存储（../08/03）；
- 识图横跨 ../01-LLM接入、../02-ReAct运行时、../04-工作区、../05-WebUI对话界面、../08-会话存储RuntimeV2；02 专项合讲准入、存储、投影、独立 API、授权和生命周期。

## 5. 版本记录

- 2026-09-20 v6：上下文压缩补入按步截断、摘要质量校验、候选流语义、可检索归档与 `history_context` 回溯原则。
- 2026-09-20 v5：新增 05《性能优化基线与已完成项》，完整映射历史完成项 3.1~3.19；识图专项扩展至 UC-9B26。
- 2026-09-20 v4：加入严格轮间性能口径与 provider/工具/本地框架分层归因原则。
- 2026-09-14 v3：修正 agent_memory 行数（1702）；版本线更新至 `d022831`。
- 2026-09-13 v1：从总清单拆分。
- 2026-09-14 v2：识图专项扩展为 UC-9B1~9B25，补入远程图、独立 API、认证、幂等、SSE、取消、结构化输出、GC、备份与前端资源生命周期。
