# 未推送改动静态消融分析（2026-09）

> 对象：`origin/master..HEAD` 的 **20 个未推送提交**（226 个文件、36 个重叠文件）。
> 方法：**静态消融**——不实际运行回退实验，而是逐提交梳理文件级重叠与依赖，推断"若单独回退（消融）该提交会破坏什么"，并标记独立性等级，为后续动态消融（基准测量）排出优先级。

## 1. 分组总览

| 簇 | 提交 | 规模 | 性质 |
|---|---|---|---|
| A. LLM 流式链路 | `850685b` 连接池+预热 · `1fd80ca` reasoning 空白 · `d9bb8b9` 热路径+间隔诊断 | 4+3+12 文件 | 核心性能/正确性 |
| B. 模型切换与熔断 | `7d64f12` 熔断重置 | 6 文件 | 核心修复 |
| C. 记忆与历史 | `f223ce6` 步区间 · `fff68de` 归档检索 | 2+4 文件 | 核心功能 |
| D. 子 Agent 前端重建 | `4083cbc` 重建 · `801abda` 文档 · `29b22cd` 目录 UI · `faab5b8` 交接/预览/锚点 | 42+5+13+14 文件 | 核心功能 |
| E. 运行生命周期 | `6fe7f39` 加固 · `48afe4f` Goal 防风暴 · `7f216db` 托盘激活 · `1d9e1b0` 侧栏回滚 · `71d443c` 附件快路径 | 14+6+3+11+3 文件 | 核心修复 |
| F. 派生（docs/tests/dist/清理） | `59478cf` 测试同步 · `f3aa4f3` 文档刷新 · `6cd82d7`/`8f6e99a` dist · `9d0a285` 探针清理 | 17+42+3+4+69 文件 | 派生，可安全保留 |

## 2. 重叠矩阵（36 个文件被 ≥2 提交触碰）

**高频热点（被 4 个提交触碰）**
- `app/agent_harness.py`：`7d64f12` `850685b` `6fe7f39` `1d9e1b0`
- `app/webui.py`：`7d64f12` `faab5b8` `6fe7f39` `1d9e1b0`

**三连重叠**
- `agent_openai.py`：`1fd80ca` `850685b` `d9bb8b9`（流式链路三个切面：**正确性/性能/诊断**，非重复）
- `agent_loop.py` / `agent_tools.py`：`850685b` `d9bb8b9`（预热与热路径协同）
- 前端窗口组：`dock/right-column.js`、`i18n.js`、`sse-handling.js`、`app.css`（`4083cbc` `faab5b8` `29b22cd`，子 Agent 重建链条）
- `session-management.js`：`4083cbc` `29b22cd` `1d9e1b0`
- dist `index.html`：两次 dist 同步（`6cd82d7` `8f6e99a`，派生）
- 三份用户方案文档：`801abda` `59478cf` `f3aa4f3`（文档迭代，派生）

**其余 25 个文件**为双边重叠（对照组/测试/dispatch），不逐一展开。

## 3. 依赖类型判定

### 3.1 硬依赖（消融会直接破坏后续提交）
| 依赖 | 证据 |
|---|---|
| `29b22cd` → `4083cbc` | `subagent-catalog-ui.js` 在 4083cbc **新建（A）**、在 29b22cd **修改（M）**——回退 4083cbc 将删除该文件，29b22cd 无从应用 |
| `801abda` → `4083cbc` | 文档明确为"addressed-conversation rebuild"重写（05/04 子代理设计），随重建口径更新 |
| dist 提交 → 前端源码提交 | `6cd82d7`/`8f6e99a` 是构建产物同步，回退任一 dist 提交会使 `check_frontend_dist_sync` 失败，但源码功能不受影响（**派生依赖**） |

### 3.2 同文件软协同（无逻辑硬依赖，但消融需处理冲突）
- `agent_harness.py` 四提交：熔断重置（7d64f12）/ LLM 预热（850685b）/ 生命周期加固（6fe7f39）/ 侧栏回滚（1d9e1b0）——各自独立改动同一大文件的不同区域。
- `agent_memory.py`：`f223ce6`（步区间）与 `fff68de`（归档集成 import）顺序演进，回退后者后前者仍自洽。
- `agent_openai.py` 三提交：功能上正交（空白保留、传输路径、间隙统计）。
- `tray_launcher.py`：`7f216db`（激活复用）与 `6fe7f39`（生命周期加固）两个修复互不依赖。

### 3.3 冗余检查结论
- **未发现功能重复实现**：三处"看似重叠"均为不同切面——
  - 流式 3 提交 = 正确性（空白）+ 性能（连接池）+ 可观测（间隙）；
  - 托盘 2 提交 = 复用重试 vs 通用加固；
  - `59478cf` 与 `f3aa4f3` 在 `test_feature_flags.py` 双触碰为两次顺序同步，非重复。
- 派生区（F 簇）占比约 **40%**（按文件数），属于"无独立功能价值、但保证一致性"的提交。

## 4. 消融矩阵（提交级）

| 提交 | 若回退的静态后果 | 独立性 |
|---|---|---|
| `850685b` LLM 连接池+预热 | 首 token 延迟回退到 SDK 每步重建连接；`warm_llm_connections` 缺失 | 独立（高价值） |
| `1fd80ca` reasoning 空白 | 思考流换行丢失 | 独立 |
| `d9bb8b9` 热路径+间隔诊断 | 每轮回调激活开销回归；间隔诊断缺失 | 独立 |
| `7d64f12` 熔断重置 | 手动切换模型可能被熔断静默跳过 | 独立 |
| `f223ce6` 步区间 | 历史尾部工具调用可被拆分 | 独立 |
| `fff68de` 归档检索 | 压缩后历史不可检索（硬依赖 `f223ce6` 同文件演进） | 软依赖 |
| `4083cbc` 子Agent前端重建 | 该链条（29b22cd/801abda）全部失效 | **硬依赖根** |
| `29b22cd` 目录 UI | 仅失去目录 UI 润色 | 依赖 4083cbc |
| `faab5b8` 交接/预览/锚点 | 子Agent交接、HTML预览、卡片锚点回退 | 独立（软协同） |
| `6fe7f39` 生命周期加固 | 隔离 WORK_DIR 被 .env 覆盖等回归 | 独立 |
| `48afe4f` Goal 防风暴 | 重复续跑风险回归 | 独立 |
| `7f216db` 托盘激活 | 通知激活再退回"偶发新开标签" | 独立 |
| `1d9e1b0` 侧栏回滚 | 陈旧快照回滚侧栏编辑回归 | 独立 |
| `71d443c` 附件快路径 | 准入性能回退（功能无变化） | 独立 |
| F 簇（docs/tests/dist/清理） | 一致性/文档回退，无功能影响 | 派生 |

## 5. 后续动态消融建议（按性价比排序）

1. **`850685b` 连接池+预热**：基准首消息 TTFT（冷进程 vs 预热后），预期差值 ≈ TCP+TLS 两段（~450ms/步）+ 提示词 ~235ms；
2. **`d9bb8b9` 热路径**：对比每轮耗时与回调激活开销（可用 `pre_api_timings` 埋点）；
3. **`fff68de` 归档检索**：长会话压缩后检索命中率/延迟；
4. **`48afe4f` Goal 防风暴**：构造并发续跑场景验证拒绝逻辑；
5. **`71d443c` 附件准入**：无附件消息的准入耗时对比。

> 方法说明：本报告为静态消融（文件级重叠+依赖推断）；动态消融（启用/禁用测量）未执行，建议按上表优先级逐项设计基准。

---
*数据来源：`git log/show --name-status`（`origin/master..HEAD` 20 提交、226 文件、36 重叠）；`--shortstat` 逐提交规模统计。*
