260730

深入分析仓库代码与最近的git相关提交和下面这些问题，解决并做好校验测试，完成后将代码分类提交：

1.模型描述加上可编辑的功能：其中 minimax不具有执行高难度任务的能力；代码和agent两项需要区分开来

2.压缩历史信息回顾方式的提示优化，写明“当用户提及与已被压缩或可能被压缩的历史信息时，使用查询工具在当前会话的session文件夹下查询events.jsonl”

3.为啥有的词发送出去会被自动翻译啊，不需要自动翻译，要让对应的前端文本分中英两种形式（如果实现复杂则都使用英语）

4.interupt模式发出的追问会单独起一个执行过程块而不是接在前面的序列里，需要是连续的执行过程块

5.待发送的追问条目在后台有时候不会自动发送，切到该会话时过一会才自动发送

6.有时候在前端页面刷新或者切换会话后前端会在底下出现很多空的连续的“回复”条目

7.goal会一闪一闪的然后重建，重建后几秒计时会从1s重新开始，过一会后又恢复成原来的计时时长，然后计时读秒也不连贯，不知道啥原因

8.goal任务某轮的轮次达到上限100退出了后会判断错误，还是继续goal任务

9.能否做一个本机电脑网络是否断联的判断，如果判断当前网络不可用就让Agent进入沉睡状态，当检测到网络恢复后恢复API请求ReAct循环。而不是一直切换模型尝试，本地网络正常但connect error才切换模型

10.有多条待发送的任务时，点击立即发送排序较后的条目有时会把第一条发出去，有时又正常

11.有时的待发送条目自动发送后会以follow up（追问）的形式新起一个执行过程块，而没有user气泡

12.在历史记录窗口点击了较早的一次对话，会导致当前正在生成中的条目加到较早一次对话后面，也就是加在历史记录的中间；然后切换会话的时候发送其他会话正在生成的执行过程块分开成单独的几块

13.滚动条是否能做成悬浮的形式，样式、位置、高亮啥的都不变只改成不占用别的组件的空间只是悬浮盖在别的组件上

14.为什么在修合盖Morden Standby之前，更早的版本却不会出现卡死的情况，是否是哪次修改导致的，或者是其他原因导致长时间无响应